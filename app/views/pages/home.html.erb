<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Send with Fuego | The Fixed-Cost Email Platform for Amazon SES</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --primary: #FF4D00;
      --primary-dark: #CC3D00;
      --secondary: #0F172A;
      --accent: #FF8C00;
      --bg-light: #FFFBF7;
      --text-main: #1E293B;
      --text-muted: #64748B;
      --gradient: linear-gradient(135deg, #FF4D00 0%, #FF8C00 100%);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Inter', -apple-system, sans-serif;
      line-height: 1.6;
      color: var(--text-main);
      background-color: white;
    }

    .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }

    /* Header / Hero */
    .hero {
      background: var(--secondary);
      color: white;
      padding: 120px 0 140px;
      text-align: center;
      position: relative;
    }

    .hero h1 {
      font-size: 4rem;
      margin-bottom: 24px;
      font-weight: 800;
      letter-spacing: -2px;
    }

    .hero h1 span {
      background: var(--gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .hero .tagline {
      font-size: 1.4rem;
      max-width: 800px;
      margin: 0 auto 48px;
      color: #CBD5E1;
      font-weight: 400;
    }

    .btn {
      padding: 18px 40px;
      border-radius: 12px;
      font-weight: 700;
      font-size: 1.1rem;
      text-decoration: none;
      transition: all 0.3s ease;
      display: inline-block;
    }

    .btn-primary {
      background: var(--primary);
      color: white;
    }

    .btn-primary:hover {
      background: var(--accent);
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(255, 77, 0, 0.3);
    }

    .btn-secondary {
      background: rgba(255,255,255,0.05);
      color: white;
      border: 1px solid rgba(255,255,255,0.2);
      margin-left: 15px;
    }

    /* Comparison Section: The Subscriber Tax */
    .comparison-section { padding: 100px 0; background: var(--bg-light); }
    
    .section-title { text-align: center; margin-bottom: 60px; }
    .section-title h2 { font-size: 2.8rem; font-weight: 800; margin-bottom: 15px; }
    .section-title p { color: var(--text-muted); font-size: 1.2rem; }

    .comparison-card {
      max-width: 950px;
      margin: 0 auto;
      background: white;
      border-radius: 24px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.05);
      display: flex;
      overflow: hidden;
      border: 1px solid #E2E8F0;
    }

    .comp-box { padding: 50px; flex: 1; }
    .comp-bad { background: #FFF5F5; border-right: 1px solid #FEE2E2; }
    .comp-good { background: white; position: relative; }

    .comp-box h4 { 
      text-transform: uppercase; 
      letter-spacing: 1.5px; 
      font-size: 0.85rem; 
      margin-bottom: 30px; 
      color: var(--text-muted);
    }

    .tax-row {
      display: flex;
      justify-content: space-between;
      padding: 15px 0;
      border-bottom: 1px solid rgba(0,0,0,0.05);
    }

    .tax-row.final { border-bottom: none; padding-top: 25px; }
    .tax-row span { font-weight: 500; color: var(--text-main); }
    
    .price-bad { color: #E53E3E; font-weight: 700; }
    .price-fuego { color: var(--primary); font-weight: 800; font-size: 1.2rem; }

    .badge-fixed {
      position: absolute; top: 20px; right: 20px;
      background: #FFEDD5; color: #9A3412;
      padding: 6px 12px; border-radius: 6px; font-size: 0.75rem; font-weight: 700;
    }

    /* How It Works: The SES Flow */
    .how-it-works { padding: 100px 0; background: white; }
    
    .steps-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 40px;
      margin-top: 60px;
    }

    .step-card { text-align: center; }
    .step-icon {
      width: 64px; height: 64px; background: var(--bg-light);
      border-radius: 16px; display: flex; align-items: center;
      justify-content: center; margin: 0 auto 24px;
      font-size: 1.5rem; border: 1px solid #FFEDD5;
    }
    .step-card h3 { margin-bottom: 12px; font-size: 1.25rem; }
    .step-card p { color: var(--text-muted); font-size: 0.95rem; }

    /* Features Grid */
    .feature-row {
      display: grid; grid-template-columns: 1fr 1fr; gap: 80px;
      align-items: center; padding: 80px 0;
    }
    .feature-content h2 { font-size: 2.5rem; margin-bottom: 20px; line-height: 1.2; }
    .feature-content p { font-size: 1.1rem; color: var(--text-muted); margin-bottom: 30px; }
    .feature-visual {
      background: var(--bg-light); border-radius: 24px;
      height: 400px; border: 1px dashed #FFD700;
      display: flex; align-items: center; justify-content: center;
    }

    /* Pricing Card Customization */
    .pricing { padding: 100px 0; background: #F8FAFC; }
    .pricing-grid {
      display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 24px;
    }
    .p-card {
      background: white; padding: 40px; border-radius: 20px;
      border: 1px solid #E2E8F0; transition: all 0.3s ease;
    }
    .p-card.featured { border: 2px solid var(--primary); transform: scale(1.05); z-index: 2; }
    .p-card:hover { transform: translateY(-5px); box-shadow: 0 20px 40px rgba(0,0,0,0.05); }
    .p-card h3 { font-size: 1.5rem; margin-bottom: 8px; }
    .p-price { font-size: 3rem; font-weight: 800; margin: 20px 0; color: var(--secondary); }
    .p-price span { font-size: 1.1rem; color: var(--text-muted); font-weight: 400; }
    .p-features { list-style: none; margin-bottom: 30px; }
    .p-features li { margin-bottom: 12px; font-size: 0.95rem; display: flex; align-items: center; }
    .p-features li::before { content: "✓"; color: var(--primary); font-weight: 800; margin-right: 12px; }

    .footer { background: var(--secondary); color: white; padding: 80px 0; text-align: center; }

    @media (max-width: 768px) {
      .hero h1 { font-size: 2.5rem; }
      .comparison-card, .feature-row { flex-direction: column; grid-template-columns: 1fr; }
      .p-card.featured { transform: scale(1); }
    }
  </style>
</head>
<body>

  <section class="hero">
    <div class="container">
      <h1>The Email Platform Your <span>Amazon SES</span> Deserves</h1>
      <p class="tagline">Stop paying a tax on your subscribers. Connect your AWS SES to Fuego and get unlimited contacts, pro analytics, and fixed-cost sending.</p>
      <div class="cta-group">
        <%= link_to "Get Started Free", new_user_registration_path, class: "btn btn-primary" %>
        <a href="#pricing" class="btn btn-secondary">See Fixed Pricing</a>
      </div>
    </div>
  </section>

  <section class="comparison-section">
    <div class="container">
      <div class="section-title">
        <h2>Kill the "Subscriber Tax"</h2>
        <p>Traditional platforms charge you more as you succeed. We don't.</p>
      </div>
      
      <div class="comparison-card">
        <div class="comp-box comp-bad">
          <h4>Traditional ESPs</h4>
          <div class="tax-row"><span>10k Contacts</span> <span class="price-bad">$150/mo</span></div>
          <div class="tax-row"><span>50k Contacts</span> <span class="price-bad">$350/mo</span></div>
          <div class="tax-row final"><span>100k+ Contacts</span> <span class="price-bad">$700+/mo</span></div>
          <p style="color: #E53E3E; font-size: 0.8rem; margin-top: 20px; font-style: italic;">Scaling cost kills your margins.</p>
        </div>
        
        <div class="comp-box comp-good">
          <span class="badge-fixed">FLAT PLATFORM FEE</span>
          <h4>The Fuego Way</h4>
          <div class="tax-row"><span>10k Contacts</span> <span class="price-fuego">$29/mo</span></div>
          <div class="tax-row"><span>50k Contacts</span> <span class="price-fuego">$29/mo</span></div>
          <div class="tax-row final"><span>100k+ Contacts</span> <span class="price-fuego">$29/mo</span></div>
          <p style="color: var(--primary); font-size: 0.8rem; margin-top: 20px; font-weight: 600;">Your cost stays flat while you grow.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="how-it-works">
    <div class="container">
      <div class="section-title">
        <h2>How Send with Fuego Works</h2>
        <p>Unlock 90% savings by bringing your own sending infrastructure.</p>
      </div>
      
      <div class="steps-grid">
        <div class="step-card">
          <div class="step-icon">🔌</div>
          <h3>Connect SES</h3>
          <p>Drop in your Amazon SES API keys. You maintain 100% ownership of your sending reputation.</p>
        </div>
        <div class="step-card">
          <div class="step-icon">👥</div>
          <h3>Import Unlimited</h3>
          <p>Upload 1,000 or 1,000,000 contacts. We never charge you for list size or storage.</p>
        </div>
        <div class="step-card">
          <div class="step-icon">🔥</div>
          <h3>Send with Fuego</h3>
          <p>Use our pro builder and canary testing to send high-volume campaigns that hit the inbox.</p>
        </div>
        <div class="step-card">
          <div class="step-icon">💰</div>
          <h3>Pay SES Directly</h3>
          <p>You pay Amazon $0.10 per 1k emails sent. No markup. No middleman fees.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="container">
    <div class="feature-row">
      <div class="feature-content">
        <h2>Predictable Margins for Agencies</h2>
        <p>Stop worrying about your clients' list sizes. With Fuego's fixed platform fee, every new subscriber your client gains is pure profit for your agency.</p>
        <ul class="p-features" style="margin-bottom: 20px;">
          <li>Unlimited Contact Storage</li>
          <li>No "Next Tier" billing surprises</li>
          <li>Sub-account management for clients</li>
        </ul>
      </div>
      <div class="feature-visual">[ Agency Dashboard Preview ]</div>
    </div>
  </section>

  <section class="pricing" id="pricing">
    <div class="container">
      <div class="section-title">
        <h2>Unlimited Contacts on Every Plan</h2>
        <p>Simple, transparent, and built to scale.</p>
      </div>

      <div class="pricing-grid">
        <div class="p-card">
          <h3>Free</h3>
          <p style="color: var(--text-muted);">For validation</p>
          <div class="p-price">$0<span>/mo</span></div>
          <ul class="p-features">
            <li style="font-weight: 700;">Unlimited Contacts</li>
            <li>10k Sends / mo</li>
            <li>30-min Safety Delay</li>
            <li>Standard Analytics</li>
          </ul>
          <%= link_to "Start Free", new_user_registration_path, class: "p-btn", style: "display:block; text-align:center; background:#F1F5F9; color:#0F172A; text-decoration:none; padding:12px; border-radius:8px; font-weight:700;" %>
        </div>

        <div class="p-card featured">
          <h3>Starter</h3>
          <p style="color: var(--text-muted);">For growth</p>
          <div class="p-price">$29<span>/mo</span></div>
          <ul class="p-features">
            <li style="font-weight: 700;">Unlimited Contacts</li>
            <li>300k Sends / mo</li>
            <li>Instant Dispatch</li>
            <li>Custom Tracking Domain</li>
            <li>Email Support</li>
          </ul>
          <%= link_to "Get Started", new_user_registration_path, class: "p-btn", style: "display:block; text-align:center; background:var(--primary); color:white; text-decoration:none; padding:12px; border-radius:8px; font-weight:700;" %>
        </div>

        <div class="p-card">
          <h3>Agency</h3>
          <p style="color: var(--text-muted);">For resellers</p>
          <div class="p-price">$129<span>/mo</span></div>
          <ul class="p-features">
            <li style="font-weight: 700;">Unlimited Contacts</li>
            <li>Unlimited Sends</li>
            <li>3 Sub-Accounts Included</li>
            <li>White-Label Interface</li>
            <li>Slack Support</li>
          </ul>
          <%= link_to "Start Agency", new_user_registration_path, class: "p-btn", style: "display:block; text-align:center; background:#0F172A; color:white; text-decoration:none; padding:12px; border-radius:8px; font-weight:700;" %>
        </div>
      </div>
    </div>
  </section>

  <footer class="footer">
    <div class="container">
      <h2>Ignite your list today.</h2>
      <p style="opacity: 0.7; margin: 20px 0 40px;">No credit card required to start on the Free plan.</p>
      <%= link_to "Create Your Account", new_user_registration_path, class: "btn btn-primary" %>
      <div style="margin-top: 80px; opacity: 0.4; font-size: 0.8rem;">
        &copy; <%= Time.current.year %> Send with Fuego. Built for high-volume marketers.
      </div>
    </div>
  </footer>

</body>
</html>