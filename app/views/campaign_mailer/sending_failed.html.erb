<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #f44336; color: white; padding: 20px; text-align: center; border-radius: 5px 5px 0 0; }
    .content { background: #f9f9f9; padding: 30px; border: 1px solid #ddd; border-top: none; border-radius: 0 0 5px 5px; }
    .alert { background: #ffebee; border-left: 4px solid #f44336; padding: 15px; margin: 20px 0; }
    .stats { background: white; padding: 20px; border-radius: 5px; margin: 20px 0; }
    .stat-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #eee; }
    .stat-label { font-weight: bold; }
    .button { display: inline-block; padding: 12px 24px; background: #f44336; color: white; text-decoration: none; border-radius: 4px; margin: 10px 0; }
    .footer { text-align: center; margin-top: 20px; font-size: 12px; color: #666; }
  </style>
</head>
<body>
  <div class="header">
    <h1>Campaign Paused</h1>
  </div>

  <div class="content">
    <p>Hi <%= @user.email %>,</p>

    <p>Your campaign <strong>"<%= @campaign.name %>"</strong> has been automatically paused due to high failure rates.</p>

    <div class="alert">
      <p><strong>Reason for Pause:</strong></p>
      <p><%= @error_details %></p>
    </div>

    <div class="stats">
      <h3>Campaign Status:</h3>
      <div class="stat-row">
        <span class="stat-label">Total Recipients:</span>
        <span><%= number_with_delimiter(@stats[:total_recipients]) %></span>
      </div>
      <div class="stat-row">
        <span class="stat-label">Successfully Sent:</span>
        <span><%= number_with_delimiter(@stats[:sent_count]) %></span>
      </div>
      <div class="stat-row">
        <span class="stat-label">Failed:</span>
        <span><%= number_with_delimiter(@stats[:failed_count]) %> (<%= number_to_percentage(@stats[:failure_rate], precision: 1) %>)</span>
      </div>
    </div>

    <p><strong>Next Steps:</strong></p>
    <ul>
      <li>Review your subscriber list for invalid email addresses</li>
      <li>Consider using an email validation service to clean your list</li>
      <li>Check your email content for spam triggers</li>
      <li>Ensure your sending domain is properly configured</li>
    </ul>

    <p style="text-align: center;">
      <a href="<%= campaign_url(@campaign) %>" class="button">View Campaign Details</a>
    </p>

    <p>Thanks,<br>
    The FuegoMail Team</p>
  </div>

  <div class="footer">
    <p>&copy; <%= Time.current.year %> <%= @account.name %> | Powered by FuegoMail</p>
  </div>
</body>
</html>
